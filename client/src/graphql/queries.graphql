query NowPlayingMovies($options: OptionsInput) {
	nowPlayingMovies(options: $options) {
		page
		total_pages
		total_results
		results {
			id
			backdrop_path
			original_title
			poster_path
			title
		}
	}
}
