import { Typography } from '@mui/material';

const Home = () => (
	<div
		style={{
			display: 'flex',
			alignItems: 'center',
			justifyContent: 'center',
			minHeight: '100vh',
		}}
	>
		<Typography variant='h3'>Home</Typography>
	</div>
);

export default Home;
